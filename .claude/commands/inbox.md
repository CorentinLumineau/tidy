Quick inbox status check:

1. Count uncategorized emails: `gog gmail search "has:nouserlabels -in:spam -in:trash" --max=100 --plain`
2. Show top 10 sender domains without labels
3. Suggest quick wins (filters that would categorize the most emails)

If cleanup is needed, ask if I want to run full `/gmail-cleanup`.
